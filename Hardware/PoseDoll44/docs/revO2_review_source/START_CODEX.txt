在 PoseDollLab_Design 的 design/revO-desktop-external 分支上继续第二轮设计。
先读取同目录 REVIEW_AND_ITERATION2_PLAN.zh-CN.md、iteration2_contract.json，以及已有 Hardware/PoseDoll44/docs/REVIEW_REQUEST_REVO.zh-CN.md。

本审查针对提交 f59e4d3a7ea3d0940b1810702cd5e1df181e0364。如果当前 HEAD 已有后续修改，先列出差异并逐项复核，不回退或覆盖用户改动。不要运行 -Stage Initialize，不覆盖 Rev N1 和本轮历史验证快照。

按报告中的优先级执行，不要只修改文档/预算数字：
1. 修复后盖实际零啮合、装配路径、预紧穿过轴向基准与轴承支承精度。先做 L6 高载荷可装配样件设计；所有新尺寸均是研究目标，必须以真实 CAD、受力与公差验证。
2. N3/N4 默认研究胸腔 MCU+近端五路板、上臂远端四路端口板；六采集节点和 44 槽语义不变。用真实元件/配合头/线尾/工具包络证明布局。若胸腔布局失败，再用报告中的全上臂折叠双板备选，不能直接放大身高。
3. 用实际 owner、质量、COM 和桌面/手托支撑工况重算关节载荷与操作力，比较双摩擦面与可追溯弹簧。不能把目录载荷当作已测保持力，也不能删未建模质量。
4. 修复相同 EPOCH 引起的 BOOT/ACK 阻塞调度，保留入网恢复、FAULT/MISSING/FIXED 和防陈旧语义。8/14ms 不变。SPI 流水只是有条件研究，未取得协议和故障等价依据不得直接替换。
5. 完善证据链：run_id 独立输出、全输入/配置/工具链哈希、预期对象完整性、依赖失败 BLOCKED。随包 review_calculations.py 仅为独立算术，不是 CAD/硬件验收器。

先闭合单关节和板位，再做多轴/单臂、七节点台架、整机和 UE。保留原物理验收阈值和 ≤1.2kg/尺寸/动作要求，不制造无证据的 PASS。供应方负责精密加工、PCBA 和线束，用户不做微型焊接和精密配合加工。

每次交付给出：改动文件、真实生成成果、原始验证结果、尚未执行项、质量/尺寸净变化（含新增件）、失败根因，以及下一步依赖。仅在需要改变用户原要求时请求决策。先不要订购整机、烧录旧板或制造放行。
